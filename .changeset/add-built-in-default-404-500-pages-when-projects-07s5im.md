---
"@emberkit/core": minor
"@emberkit/cli": patch
---

Add built-in default 404/500 pages when projects omit custom routes. Merge Head shorthand with children; fix SSR head priority and prerender template pollution. Wire notFoundRoute and errorRoute through virtual routes, client render, and CLI templates.

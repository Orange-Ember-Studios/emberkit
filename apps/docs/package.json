{
  "name": "@emberkit/docs",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "emberkit dev",
    "build": "emberkit build",
    "preview": "emberkit preview",
    "deploy": "pnpm --filter @emberkit/cli build && wrangler deploy",
    "deploy:preview": "wrangler deploy --preview"
  },
  "dependencies": {
    "@emberkit/cli": "^0.2.3",
    "@emberkit/core": "^0.2.4-alpha.0",
    "@emberkit/icons": "^0.2.1-alpha.1",
    "@emberkit/ui": "^0.2.3-alpha.1",
    "@tailwindcss/vite": "^4.3.0",
    "tailwindcss": "^4.3.0",
    "vite": "^6.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.90.1"
  }
}